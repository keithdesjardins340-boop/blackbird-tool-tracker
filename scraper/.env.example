# Copy to .env for local runs. In GitHub Actions these come from repo secrets.
SUPABASE_URL=https://ssfhjhbarkpgbelnbcun.supabase.co
# service_role key (NOT the anon key) — scrapers need write access.
# Supabase → Project Settings → API → service_role. Never commit this.
SUPABASE_SERVICE_ROLE_KEY=
